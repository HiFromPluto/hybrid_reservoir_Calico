AHL, acid, and task sidecars. Paths in config/*.properties are relative
to the HybridDish folder (the working directory).

  ahl_narma10.txt      Frozen NARMA-10 drive, u ~ Uniform[0, 0.5]
  acid_held05.txt      Acid held at 0.5 every analysis window
  narma10_target.csv   Window; u; y_next  (semicolon)
  ahl_waveform.txt     Optional sine/square/triangle drive
  waveform_labels.csv  Optional 3-class labels (0=sine, 1=square, 2=triangle)

Do not regenerate these after looking at readout scores.
